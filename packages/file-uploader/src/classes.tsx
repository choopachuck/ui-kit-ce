export type DropzoneClasses = {
  /**
   * Стиль, применяемый к input
   */
  input?: string
  /**
   * Стиль, применяемый к контейнеру Dropzone
   */
  dropzone?: string
  /**
   * Стиль, применяемый к контейнеру при dragEnter'е
   */
  dragEnter?: string
  /**
   * Стиль при disabled
   */
  disabled?: string
  /**
   * Стиль при error=true
   */
  error?: string
}

export type UploaderLabelClasses = {
  /**
   * Стиль для label
   */
  label?: string
  /**
   * Стиль для ошибки
   */
  error?: string
  /**
   * Стиль для описания
   */
  description?: string
  /**
   * Стиль для disabled
   */
  disabled?: string
}
