import { Meta, ArgsTable } from '@storybook/addon-docs'
import { Checkbox } from '@v-uik/checkbox'
import { CheckboxGroup } from '@v-uik/checkbox-group'
import { createTitle, COMPONENTS } from '../../docs/showroom/config'

import { ClassesDummy } from '@v-uik/checkbox/examples'

<Meta
  title={createTitle([COMPONENTS.controls, 'Checkbox', 'API'])}
  component={Checkbox}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# Checkbox API

<ArgsTable of={Checkbox} />

# Classes

<ArgsTable of={ClassesDummy} />

# CheckboxGroup API

<ArgsTable of={CheckboxGroup} />
