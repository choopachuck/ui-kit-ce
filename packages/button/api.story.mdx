import { Meta, ArgsTable } from '@storybook/addon-docs'
import { Button } from '@v-uik/button'
import { createTitle, COMPONENTS } from '../../docs/showroom/config'
import { ClassesDummy } from './examples'

<Meta
  title={createTitle([COMPONENTS.controls, 'Button', 'API'])}
  component={Button}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# API

<ArgsTable of={Button} />

# Classes

<ArgsTable of={ClassesDummy} />
