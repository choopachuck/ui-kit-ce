import { Meta, ArgsTable } from '@storybook/addon-docs'
import { Popup } from '@v-uik/popup'
import { createTitle, COMPONENTS } from '../../docs/showroom/config'

<Meta
  title={createTitle([COMPONENTS.utility, 'Popup', 'API'])}
  component={Popup}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# API

<ArgsTable of={Popup} />
