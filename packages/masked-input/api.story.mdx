import { Meta, ArgsTable } from '@storybook/addon-docs'
import { MaskedInput } from '@v-uik/masked-input'
import { createTitle, COMPONENTS } from '../../docs/showroom/config'

<Meta
  title={createTitle([COMPONENTS.inputFields, 'MaskedInput', 'API'])}
  component={MaskedInput}
  arameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# API

<ArgsTable of={MaskedInput} />

# Classes

Набор классов MaskedInput совпадает с набором [классов Input](?path=/docs/поля-ввода-input-api--page#input-classes)
