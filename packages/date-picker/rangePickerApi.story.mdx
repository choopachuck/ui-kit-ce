import { ArgsTable, Meta } from '@storybook/addon-docs'
import { RangePicker } from '@v-uik/date-picker'
import { COMPONENTS, createTitle } from '../../docs/showroom/config'
import { RangePickerClassesDummy } from './examples'

<Meta
  title={createTitle([COMPONENTS.inputFields, 'RangePicker', 'API'])}
  component={RangePicker}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# RangePicker API

<ArgsTable of={RangePicker} />

# RangePicker classes

<ArgsTable of={RangePickerClassesDummy} />
