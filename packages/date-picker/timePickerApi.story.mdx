import { Meta, ArgsTable } from '@storybook/addon-docs'
import { TimePicker } from '@v-uik/date-picker'
import { createTitle, COMPONENTS } from '../../docs/showroom/config'

<Meta
  title={createTitle([COMPONENTS.inputFields, 'TimePicker', 'API'])}
  component={TimePicker}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# TimePicker API

<ArgsTable of={TimePicker} />

# TimeRangePicker API

<ArgsTable of={TimePicker.RangePicker} />
