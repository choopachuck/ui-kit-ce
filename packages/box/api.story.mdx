import { Meta, ArgsTable } from '@storybook/addon-docs'
import { Box } from '@v-uik/box'
import { createTitle, COMPONENTS } from '../../docs/showroom/config'

<Meta
  title={createTitle([COMPONENTS.utility, 'Box', 'API'])}
  component={Box}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# API

<ArgsTable of={Box} />
