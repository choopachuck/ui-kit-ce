import { Canvas, Meta, Source } from '@storybook/addon-docs'
import { Table } from '@v-uik/table'
import { COMPONENTS, createTitle } from '../../../docs/showroom/config'
import { HoverableStory } from '@v-uik/table/examples/HoverableStory'
import RawHoverableStory from '!!raw-loader!@v-uik/table/examples/HoverableStory'

<Meta
  title={createTitle([
    COMPONENTS.dataDisplay,
    'Table',
    'Индикация строк при наведении курсора',
  ])}
  component={Table}
/>

# Индикация строк при наведении курсора

Свойство `hoverable` включает индикацию строки при наведении на нее курсора.

<Canvas withSource="none">
  <HoverableStory />
</Canvas>

<Source language="tsx" code={RawHoverableStory} />
