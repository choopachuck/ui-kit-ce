import { Canvas, Meta, Source } from '@storybook/addon-docs'
import { Table } from '@v-uik/table'
import { COMPONENTS, createTitle } from '../../../docs/showroom/config'
import { StripeStory } from '@v-uik/table/examples/StripeStory'
import RawStripeStory from '!!raw-loader!@v-uik/table/examples/StripeStory.tsx'

<Meta
  title={createTitle([COMPONENTS.dataDisplay, 'Table', 'Полосатая таблица'])}
  component={Table}
/>

# Полосатая таблица

Свойство `stripe` включает оформление полосатой таблицы, где выделяется каждая четная строка.

<Canvas withSource="none">
  <StripeStory />
</Canvas>

<Source language="tsx" code={RawStripeStory} />
