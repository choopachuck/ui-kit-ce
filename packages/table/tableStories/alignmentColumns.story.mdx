import { Canvas, Meta, Source } from '@storybook/addon-docs'
import { Table } from '@v-uik/table'
import { COMPONENTS, createTitle } from '../../../docs/showroom/config'
import { AlignmentStory } from '@v-uik/table/examples/AlignmentStory'
import RawAlignmentStory from '!!raw-loader!@v-uik/table/examples/AlignmentStory.tsx'

<Meta
  title={createTitle([
    COMPONENTS.dataDisplay,
    'Table',
    'Положение контента в столбцах',
  ])}
  component={Table}
/>

# Положение контента в столбцах

В объекте описания колонки `columns` с помощью свойства `align` можно указать, как будет расположен
контент в нужном столбце таблицы. По умолчанию свойство `align` имеет значение `left`.

<Canvas withSource="none">
  <AlignmentStory />
</Canvas>

<Source language="tsx" code={RawAlignmentStory} />
