export type Classes = {
  /** Стиль, применяемый к обертке */
  container?: string
  /** Стиль, применяемый к элементу с `error='true'` */
  error?: string
  /** Стиль, применяемый к элементу с `disabled='true'` */
  disabled?: string
  /** Стиль, применяемый к группе чек-боксов */
  checkboxGroup?: string
  /** Стиль, применяемый к элементу с `direction='vertical'` */
  vertical?: string
}
