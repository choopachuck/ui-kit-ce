import { Meta, ArgsTable } from '@storybook/addon-docs'
import { InputNumber } from '@v-uik/input-number'
import { createTitle, COMPONENTS } from '../../docs/showroom/config'

<Meta
  title={createTitle([COMPONENTS.inputFields, 'InputNumber', 'API'])}
  component={InputNumber}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# API

<ArgsTable of={InputNumber} />

# Classes

Набор классов InputNumber совпадает с набором [классов Input](?path=/docs/элементы-управления-button-api--page#classes)
