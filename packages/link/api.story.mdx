import { Meta, ArgsTable } from '@storybook/addon-docs'
import { Link } from '@v-uik/link'
import {
  createTitle,
  COMPONENTS,
  createStory,
} from '../../docs/showroom/config'

<Meta
  title={createTitle([COMPONENTS.navigation, 'Link', 'API'])}
  component={Link}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# API

<ArgsTable of={Link} />
