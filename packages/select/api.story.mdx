import { Meta, ArgsTable } from '@storybook/addon-docs'
import { Select } from '@v-uik/select'
import { createTitle, COMPONENTS } from '../../docs/showroom/config'

import { OptionDummy, ClassesDummy } from './examples'

<Meta
  title={createTitle([COMPONENTS.inputFields, 'Select', 'API'])}
  component={Select}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# Select API

<ArgsTable of={Select} />

# Интерфейс Option

Тип `Option` наследуется от `ListItemProps`, поэтому может принимать свойства компонента `ListItem`.

# Select classes

<ArgsTable of={ClassesDummy} />

# Option API

<ArgsTable of={OptionDummy} />
