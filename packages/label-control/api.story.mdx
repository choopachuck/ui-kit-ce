import { Meta, ArgsTable } from '@storybook/addon-docs'
import { LabelControl } from '@v-uik/label-control'
import { createTitle, COMPONENTS } from '../../docs/showroom/config'
import { ClassesDummy } from './examples'

<Meta
  title={createTitle([COMPONENTS.utility, 'LabelControl', 'API'])}
  component={LabelControl}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# API

<ArgsTable of={LabelControl} />

# Classes

<ArgsTable of={ClassesDummy} />
