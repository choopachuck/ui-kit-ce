import { ReactNode } from 'react'
import { CreatableOwnProps } from '../../src/hooks'

export function DocsCreatable<Option>({
  // eslint-disable-next-line
  newOptionPosition = 'last',
}: CreatableOwnProps<Option>): ReactNode {
  return null
}
