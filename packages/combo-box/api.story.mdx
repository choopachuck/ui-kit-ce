import { ArgsTable, Meta } from '@storybook/addon-docs'
import { ComboBox, Creatable } from '@v-uik/combo-box'
import { COMPONENTS, createTitle } from '../../docs/showroom/config'

import { ClassesDummy } from './examples'

import { DocsCreatable } from './examples/assets/DocsCreatable'

<Meta
  title={createTitle([COMPONENTS.inputFields, 'ComboBox', 'API'])}
  component={ComboBox}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# ComboBox API

<ArgsTable of={ComboBox} />

# Creatable API

<ArgsTable of={DocsCreatable} />

# Classes

<ArgsTable of={ClassesDummy} />
