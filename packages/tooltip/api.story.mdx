import { Meta, ArgsTable } from '@storybook/addon-docs'
import { Tooltip } from '@v-uik/tooltip'
import { createTitle, COMPONENTS } from '../../docs/showroom/config'
import { ClassesDummy } from './examples'

<Meta
  title={createTitle([COMPONENTS.feedback, 'Tooltip', 'API'])}
  component={Tooltip}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# Tooltip API

<ArgsTable of={Tooltip} />

# Classes

<ArgsTable of={ClassesDummy} />
