import { Meta, ArgsTable } from '@storybook/addon-docs'
import { Radio } from '@v-uik/radio'
import { RadioGroup } from '@v-uik/radio-group'
import { createTitle, COMPONENTS } from '../../docs/showroom/config'
import { ClassesDummy } from './examples'

<Meta
  title={createTitle([COMPONENTS.controls, 'Radio', 'API'])}
  component={RadioGroup}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# Radio API

<ArgsTable of={Radio} />

# RadioGroup API

<ArgsTable of={RadioGroup} />

# Radio classes

<ArgsTable of={ClassesDummy} />
