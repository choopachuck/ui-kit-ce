import { Meta, ArgsTable } from '@storybook/addon-docs'
import { LinearProgress, CircularProgress } from '@v-uik/progress'
import { createTitle, COMPONENTS } from '../../docs/showroom/config'
import { CircularClassesDummy, LinearClassesDummy } from './examples'

<Meta
  title={createTitle([COMPONENTS.feedback, 'Progress', 'API'])}
  component={LinearProgress}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# LinearProgress API

<ArgsTable of={LinearProgress} />

# LinearProgress Classes

<ArgsTable of={LinearClassesDummy} />

# CircularProgress API

<ArgsTable of={CircularProgress} />

# LinearProgress Classes

<ArgsTable of={CircularClassesDummy} />
