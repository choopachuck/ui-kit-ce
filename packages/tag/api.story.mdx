import { Meta, ArgsTable } from '@storybook/addon-docs'
import { Tag, PolymorphicTag } from '@v-uik/tag'
import { createTitle, COMPONENTS } from '../../docs/showroom/config'
import { TagClassesDummy, PolymorphicTagDummy } from './examples'

<Meta
  title={createTitle([COMPONENTS.controls, 'Tag', 'API'])}
  component={Tag}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# Tag API

<ArgsTable of={Tag} exclude="placeholder" />

# Tag classes

<ArgsTable of={TagClassesDummy} />{' '}

# PolymorphicTag API

<ArgsTable of={PolymorphicTagDummy} />

# PolymorphicTag Classes

Набор классов PolymorphicTag совпадает с набором [классов компонента Tag](?path=/docs/элементы-управления-tag-api--page#tag-classes)
