import { Meta, ArgsTable } from '@storybook/addon-docs'
import { Textarea } from '@v-uik/textarea'
import { createTitle, COMPONENTS } from '../../docs/showroom/config'
import { ClassesDummy } from './examples'

<Meta
  title={createTitle([COMPONENTS.inputFields, 'Textarea', 'API'])}
  component={Textarea}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# Textarea API

<ArgsTable of={Textarea} />

# Textarea classes

<ArgsTable of={ClassesDummy} />
