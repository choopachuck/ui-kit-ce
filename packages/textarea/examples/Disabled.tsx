import * as React from 'react'
import { Textarea } from '@v-uik/base'

export const Disabled = (): React.ReactElement => {
  return <Textarea disabled value="There is no area better than Tsaritsyno" />
}
